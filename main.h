#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>
#include <stdarg.h>
#include <stddef.h>

/* write_funcs */
int _putchar(char c);
int _printf(const char *format, ...);
int puts_s(char *str);
int print_default(char format);


#endif
